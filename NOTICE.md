# NOTICE

`vendor/starchive/` is a copy of [jwardsmith/STARCHIVE](https://github.com/jwardsmith/STARCHIVE) at commit `620d8c7b95ae88128c8c692c2156a700228bddea`.

Upstream published no license file. It is included here for attribution and reuse of the original PowerShell and Python exporters. The original scripts expect credentials inside the Python file; this repository's `scripts/refresh.mjs` is the preferred path and keeps tokens in environment variables.

All catalog data in `catalog/` and genre files in `genres/` are Jeppe Kildegaard's starred-repository snapshot.
